#include <raylib.h>
#include "defines.h"
#include "movement.h"


  /*//Variáveis de controle para animação das asas
    int frameAtual = 0;
    int timer = 0;
    int frameSpeed = 10;


movementprincipal(int speedpassaro){


        if (IsKeyPressed(KEY_SPACE)){
            speedpassaro = jumpForce;
        }
        return speedpassaro;
}

/*
void resto(){
        //Gravidad
        if (speedpassaro < 0){
            //Subida
            speedpassaro += gravidade * gravidade;
        }
        else{
            //Descida
            speedpassaro += gravidade * gravidadedobro;
        }

        //Limitação da velocidade de descida
        if (speedpassaro > 10.0f){
            speedpassaro = 12.0f;
        }

        //Movimento do pássaro
        posY += speedpassaro;

        //Asas batendo
        timer ++;
        if (timer >= frameSpeed){
            frameAtual++;
            if (frameAtual > 2){
                frameAtual = 0;
            }
            timer = 0;
        }

}*/
